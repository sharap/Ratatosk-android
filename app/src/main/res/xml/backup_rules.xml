<?xml version="1.0" encoding="utf-8"?>
<!--
    То же самое для Android 9–11 (API 28–30), где действуют эти правила,
    а не `data_extraction_rules.xml`.

    На этих версиях всё решает `android:allowBackup="false"` в манифесте,
    и до сюда дело не доходит. Файл оставлен заполненным намеренно: если
    бэкап когда-нибудь включат обратно, он не должен молча забрать с собой
    базу переписки — ту, что на другом устройстве всё равно не открыть
    (ключ лежит в Android Keystore и никуда не едет).
-->
<full-backup-content>
    <exclude domain="root" path="." />
    <exclude domain="file" path="." />
    <exclude domain="database" path="." />
    <exclude domain="sharedpref" path="." />
    <exclude domain="external" path="." />
    <exclude domain="device_root" path="." />
    <exclude domain="device_file" path="." />
    <exclude domain="device_database" path="." />
    <exclude domain="device_sharedpref" path="." />
</full-backup-content>
